# --- Postgres (Neon, via Vercel's Marketplace integration — the direct
# successor to the old "Vercel Postgres" product, same Storage tab) ---
# Add a Postgres/Neon integration in your Vercel dashboard -> Storage tab.
# Vercel injects DATABASE_URL automatically once it's connected to this
# project; copy it here for local dev.
DATABASE_URL=""

# --- Admin auth (single admin user, no external service needed) ---
# Generate a bcrypt hash of your chosen password, e.g. via
# node -e "console.log(require('bcryptjs').hashSync('yourpassword', 10))"
ADMIN_EMAIL="you@example.com"
ADMIN_PASSWORD_HASH=""
# Any long random string — used to sign the admin session cookie.
ADMIN_SESSION_SECRET="change-me-to-a-long-random-string"

# --- Cashfree (sandbox first, swap to production values at launch) ---
CASHFREE_APP_ID=""
CASHFREE_SECRET_KEY=""
CASHFREE_ENV="SANDBOX" # SANDBOX | PRODUCTION
CASHFREE_WEBHOOK_SECRET=""

# --- Public site URL (used for Cashfree return_url + OG tags) ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
# Mirrors CASHFREE_ENV but exposed client-side for the Cashfree JS SDK.
NEXT_PUBLIC_CASHFREE_ENV="SANDBOX"

# --- Meta Pixel + Conversions API ---
# All optional. If any is unset, the affected piece silently no-ops —
# nothing else breaks. NEXT_PUBLIC_META_PIXEL_ID and META_PIXEL_ID MUST be
# set to the SAME pixel ID (see src/lib/meta-config.ts): they drive the
# browser pixel and the server-side Conversions API call respectively, and
# Meta can only dedupe a Purchase between the two when both agree.
# Browser-facing pixel ID — embedded in the page for fbq(). From Meta
# Events Manager > your pixel > Settings > the ID at the top.
NEXT_PUBLIC_META_PIXEL_ID=""
# Server-side pixel ID — used only by the Conversions API call in
# src/lib/meta-capi.ts. Same value as NEXT_PUBLIC_META_PIXEL_ID above.
META_PIXEL_ID=""
# Conversions API access token. Same page as above > Conversions API >
# Generate access token.
META_CAPI_TOKEN=""
